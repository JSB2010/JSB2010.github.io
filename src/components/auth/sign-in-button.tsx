"use client";

// This is an empty component that doesn't render anything
// We're keeping it to avoid breaking imports, but it doesn't show a sign-in button
export function SignInButton() {
  return null;
}
